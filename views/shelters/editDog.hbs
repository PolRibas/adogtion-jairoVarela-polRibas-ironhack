{{>navShelter}}
<section class="user-profile">
    <center><form action="/app/changePhoto/{{dog._id}}" method="POST" enctype="multipart/form-data">
        <label for="image">
            <input onchange="this.form.submit()" type="file" name="image" id="image" style="display:none;"/>
            <img src="{{dog.image}}" class="profile-img"/>
         </label>
    </form>
    <h2>{{dog.name}}</h2></center>
    <p>Breed:{{dog.breed}}</p>
    <p>Size: {{dog.size}}</p>
    <p>Gender: {{dog.gender}}</p>
    <p>age: {{dog.age}}</p>
    <p>age: {{dog.age}}</p>
    <p>weight: {{dog.weight}}</p>
</section>
    {{>dogUpdate}}
    {{>confirmDeleteDog}}


